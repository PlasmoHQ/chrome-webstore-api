{
  "name": "@plasmo-corp/cwu",
  "version": "1.8.0",
  "description": "Chrome webstore upload library",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "run-p dev:*",
    "dev:type": "tsc -w --emitDeclarationOnly",
    "dev:code": "esbuild index.ts --bundle --watch --platform=node --outdir=dist",
    "build": "run-p build:*",
    "build:type": "tsc --emitDeclarationOnly",
    "build:code": "esbuild index.ts --bundle --minify --platform=node --outdir=dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "prepare": "run-s clean build"
  },
  "author": "Plasmo Corp. <support@plasmo.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/plasmo-corp/cwu.git"
  },
  "keywords": [
    "browser-extensions",
    "chrome",
    "webstore",
    "browser-plugins"
  ],
  "license": "MIT",
  "devDependencies": {
    "@plasmo-corp/rps": "1.1.0",
    "@trivago/prettier-plugin-sort-imports": "3.2.0",
    "@types/node": "17.0.21",
    "esbuild": "0.14.23",
    "prettier": "2.5.1",
    "rimraf": "3.0.2",
    "typescript": "4.6.3"
  },
  "dependencies": {
    "got": "12.0.1"
  }
}
