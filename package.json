{
  "name": "@plasmo-corp/cwu",
  "version": "1.7.0",
  "description": "Chrome webstore upload library, soft-fork of https://github.com/fregante/chrome-webstore-upload",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "esbuild index.ts --bundle --watch --platform=node --outdir=dist",
    "build": "esbuild index.ts --bundle --minify --platform=node --outdir=dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "prepare": "run-s clean build"
  },
  "author": "Plasmo Corp. <support@plasmo.com>",
  "license": "MIT",
  "devDependencies": {
    "@plasmo-corp/rps": "^1.1.0",
    "@trivago/prettier-plugin-sort-imports": "3.2.0",
    "@types/node": "17.0.18",
    "esbuild": "^0.14.23",
    "prettier": "2.5.1",
    "rimraf": "^3.0.2",
    "typescript": "4.5.5"
  },
  "dependencies": {
    "got": "12.0.1"
  }
}
